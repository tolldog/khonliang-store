"""User-side CLIs that produce inputs for khonliang agents to consume.

Today: ``store.cli.stage`` (``kh-stage``) — captures byte-shaped agent
inputs (diff today; changed-files / module / repo / with-context to follow)
and emits a routable handle of shape ``<backend>:<id>``.

Tracked under ``fr_khonliang-bus-lib_520ce3bf`` (FR retargeted to
khonliang-store; FR id kept for stability).
"""
